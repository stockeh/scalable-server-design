plugins {
    id 'java'
    id "org.sonarqube" version "2.7"
}

sourceSets {
    main {
         java {
            srcDirs = ['src']
         }
    }
}

sonarqube {
  properties {
    sonar.projectKey: stockeh_scalable-server-design
  }
}
