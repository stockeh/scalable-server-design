plugins {
    id 'java'
    id "org.sonarqube" version "2.7"
}

sourceSets {
    main {
         java {
            srcDirs = ['src']
         }
    }
}

sonarqube {
    properties {
        property "sonar.projectKey", "stockeh_scalable-server-design"
    }
}
